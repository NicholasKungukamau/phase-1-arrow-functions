function divide(){
    let par1, par2;
    return (par1 / par2);

}
 divide(2000, 100);

 let x = 2
 const square= x ** x


function add (){
    const addi= (para1, para2) => para1 + para2;
}
add(3, 4);